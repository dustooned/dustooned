---
type Props = {
  href: string;
  label: string;
};

const { href, label } = Astro.props;
---

<a class="button-link" href={href} target="_blank" rel="noopener noreferrer">
  {label}
  <span aria-hidden="true">&nearr;</span>
  <span class="visually-hidden"> opens in a new tab</span>
</a>
